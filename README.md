
# CueCube wholesale site (static + ShipStation rates)

## Local dev

```bash
npm install
export SHIPSTATION_API_KEY=your_key
export SHIPSTATION_API_SECRET=your_secret
npm start
# open http://localhost:3000
```

Demo login: `demo@cuecube.com` / `Qwerty142536`

### Render
- Build: `npm install`
- Start: `npm start`
- Region: US East (close to ShipStation)
- Instance: Starter
- Environment:
  - `SHIPSTATION_API_KEY`
  - `SHIPSTATION_API_SECRET`
